type Query {
  hello: String!
  helloWithName(name: String): String!
  peopleNumber: Int!
  bookList: [Book!]!
}
# Información de los libros
type Book {
    id: ID!
    title: String!
    isbn: String!
    pageCount: Int!
    publishedDate: String
    thumbnailUrl: String
    status: String
    authors: [String!]!
  
}
